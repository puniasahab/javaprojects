package strategy.botStrategy;

import models.Board;
import models.Cell;

public class HardPlayingStrategy implements BotStrategy{
    @Override
    public Cell decideMove(Board b) {
        return null;
    }
}
